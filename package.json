{
  "name": "ws-auction",
  "version": "1.0.0",
  "main": "dist/server.js",
  "scripts": {
    "build": "npm install --include=dev && tsc && npm run build:client",
    "build:client": "cd client && npm install && npm run build",
    "start": "node dist/server.js",
    "dev": "ts-node src/server.ts"
  },
  "dependencies": {
    "@types/node": "^20.0.0",
    "@types/pg": "^8.20.0",
    "@types/ws": "^8.5.10",
    "ioredis": "^5.11.0",
    "pg": "^8.21.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {}
}
